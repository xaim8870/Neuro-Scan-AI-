## Packages
recharts | For visualizing model accuracy and loss graphs
framer-motion | For smooth page transitions and entry animations
clsx | For conditional class merging
tailwind-merge | For merging tailwind classes in components
lucide-react | For medical icons (already in base, but good to note usage)

## Notes
API: POST /api/analyze expects FormData with 'image' field
Response: JSON with diagnosis, confidence, and imageUrl
Tailwind Config: Extended font families will be needed for the medical aesthetic
